# Task 6 clean re-acceptance round 2

**Objective:** Task-6 fix-round-2 clean re-acceptance identity `task6-round2-clean-20260824` — verify six linear workstream terminals without operator SQL or chown.

## Mission

Progressive dependency-gated execution only; honest terminal states when harness CLIs are absent.

## P0 authority envelope

Allowed:

- Submit and execute within disposable TOP-DELIVERY control plane scope.

Forbidden without a new explicit authority envelope:

- Operator SQL to mutate parent task states.
- Manual chown on artifact directories.
- Broker mutation.

## Ordered workstreams

### 1. Re-acceptance slice one

Minimal scoped slice.

### 2. Re-acceptance slice two

Minimal scoped slice.

### 3. Re-acceptance slice three

Minimal scoped slice.

### 4. Re-acceptance slice four

Minimal scoped slice.

### 5. Re-acceptance slice five

Minimal scoped slice.

### 6. Re-acceptance slice six

Minimal scoped slice.

## Cross-workstream acceptance matrix

| Item | Required terminal disposition |
|---|---|
| 1. Re-acceptance slice one | `BLOCKED` |
| 2. Re-acceptance slice two | `BLOCKED` |
| 3. Re-acceptance slice three | `BLOCKED` |
| 4. Re-acceptance slice four | `BLOCKED` |
| 5. Re-acceptance slice five | `BLOCKED` |
| 6. Re-acceptance slice six | `BLOCKED` |
