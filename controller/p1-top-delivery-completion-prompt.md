Use $terra-delivery and the required role skills.

P1-TOP-DELIVERY-COMPLETION

Create one autonomous run to complete every TOP-DELIVERY capability that is not already fully functional. Continue through implementation, adversarial review, remediation, tests, disposable validation, deployment, restart recovery, Signal delivery and authenticated QA without intermediate prompts. Retry queueable failures, rotate only through the approved model-routing policy, clear stale writers, preserve evidence, and loop until every acceptance assertion passes.

Complete and verify: the parent task scheduler; durable child leases; stale-worker watchdog; persistent Signal daemon; persistent Signal notifier; Hermes command adapter; Comms-01 controller socket deployment; and all required TOP-DELIVERY/Longspan integration. Keep existing active/done capabilities unchanged.

Require real end-to-end Signal status delivery, read-only commands, action-bound six-digit 2FA for queued execution, restart recovery, duplicate/replay rejection, unauthorized-sender rejection, status heartbeats, stale-worker recovery, evidence indexing, encrypted backup and restore rehearsal. Do not claim completion from active systemd units alone.

Never publish secrets, expose private messages, place broker orders, mutate production, ledger, production databases, staging state, network routing or prior runs. Deploy only reviewed exact-SHA artifacts from a clean checkout. Stop only for credential compromise, cryptographic failure, data loss, rollback failure, wrong-target ambiguity or catastrophic infrastructure failure. Finish with exact files, SHAs, service/PID state, Signal evidence, tests, rollback evidence, residual risks and the next project outcome.
