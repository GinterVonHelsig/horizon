P1-FOREGROUND-COMMS01-RECOVERY

Work interactively in the current session. Do not delegate this run to an opaque background agent or emit a continuation prompt. Show each meaningful action and result before proceeding.

Resume run 20260810T004319Z-b626467e only from the last accepted isolated phase-3A worktree state. Preserve all accepted and rejected patch evidence. Repair the rejected controller API batch directly, with exact existing interfaces and no invented compatibility layer. Run focused tests after each change, then independent source review, Signal/Hermes boundary tests, service restart recovery tests, and authenticated read-only QA.

Start and configure Hermes plus the persistent TOP-DELIVERY Signal notifier on comms-01 only after the isolated implementation passes. Bind transport locally, allow only the verified operator identity, reject groups and unknown senders, and send concise status updates on start, retry, phase transition, acceptance, rejection and terminal state. Do not include secrets, codes, raw prompts or sensitive payloads.

Verify read-only Signal commands for run status, architecture-summary, artifact lookup and next step. Verify 2FA-protected queue commands with expiry, replay, wrong-action and unauthorized-sender tests. Keep execution inert until all tests pass. Never modify production, brokers, ledger, production databases, network, staging state or prior-run history.

Finish with the exact files changed, tests, service state, Signal delivery evidence, remaining blocker and next foreground action.
